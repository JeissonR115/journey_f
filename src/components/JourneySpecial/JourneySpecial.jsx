import { Layout } from "../../Layout";
export const JourneySpecial = () => (
  <Layout>
    {
      // crea un boton para crear un nuevo joruney
      // al darle click al boton deve lanzar un poopup en donde este un formulario en donde tenga los sigientes datos
      //fecha de solicitud, tipode usuario(empleado, externo), usuario requiere servicio, usuario aprueba, origen, destino, fecha servicio, hora servicio, centro de costo;
    }
  </Layout>
);
